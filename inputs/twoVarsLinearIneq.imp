class twoVarsLinearIneq {

    basic(x) {
        def y = x + 1;
        def z = y + 2;
        if (x <= z) {
            def w = z;
        }
    }

    complex(a) {
        def b = a + 1;
        def c = b;
        if (b <= c) {
            c = a + 2;
        }
        def d = c + 1;
    }

}